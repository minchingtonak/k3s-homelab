apiVersion: v1
kind: Namespace
metadata:
  name: watcharr
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: watcharr-data
  namespace: watcharr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: watcharr
  namespace: watcharr
  labels:
    app.kubernetes.io/name: watcharr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: watcharr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: watcharr
    spec:
      containers:
        - name: watcharr
          image: ghcr.io/sbondco/watcharr:v4.0.1@sha256:53514a8b896b08d8f19f2ee9548ff2fc59c1ecedbbe5ab012fb91fc394859335
          ports:
            - containerPort: 3080
              name: http
          env:
            - name: TZ
              value: "America/New_York"
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: watcharr-data
---
apiVersion: v1
kind: Service
metadata:
  name: watcharr
  namespace: watcharr
  annotations:
    gatus.home-operations.com/endpoint: |
      alerts:
        - type: pushover
spec:
  selector:
    app.kubernetes.io/name: watcharr
  ports:
    - port: 80
      targetPort: 3080
      name: http
