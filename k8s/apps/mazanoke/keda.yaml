apiVersion: http.keda.sh/v1beta1
kind: InterceptorRoute
metadata:
  name: mazanoke
  namespace: mazanoke
spec:
  target:
    service: mazanoke
    port: 80
  rules:
    - hosts:
        - mazanoke.item.fyi
  scalingMetric:
    concurrency:
      targetValue: 30
  timeouts:
    request: 120s
    responseHeader: 60s
    readiness: 90s
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: mazanoke
  namespace: mazanoke
spec:
  scaleTargetRef:
    name: mazanoke
  minReplicaCount: 0
  maxReplicaCount: 1
  cooldownPeriod: 600
  triggers:
    - type: external-push
      metadata:
        scalerAddress: keda-add-ons-http-external-scaler.keda:9090
        interceptorRoute: mazanoke
