apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: syncthing
  namespace: syncthing
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "Syncthing"
    gethomepage.dev/description: "File synchronization"
    gethomepage.dev/group: "Tools"
    gethomepage.dev/icon: "syncthing.png"
    gethomepage.dev/href: "https://syncthing.item.fyi"
    gethomepage.dev/siteMonitor: "https://syncthing.item.fyi"
    gethomepage.dev/pod-selector: "app.kubernetes.io/name=syncthing"
    gethomepage.dev/widget.type: "syncthing"
    gethomepage.dev/widget.url: "http://syncthing.syncthing.svc.cluster.local"
    gethomepage.dev/widget.key: "{{HOMEPAGE_VAR_SYNCTHING_API_KEY}}"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`syncthing.item.fyi`)
      middlewares:
        - name: authentik
          namespace: authentik
      services:
        - name: syncthing
          port: 80
  tls: {}
