apiVersion: http.keda.sh/v1beta1
kind: InterceptorRoute
metadata:
  name: ytptube
  namespace: ytptube
spec:
  target:
    service: ytptube
    port: 80
  rules:
    - hosts:
        - ytptube.item.fyi
  scalingMetric:
    concurrency:
      targetValue: 30
  timeouts:
    request: 120s
    responseHeader: 60s
    readiness: 90s
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: ytptube
  namespace: ytptube
spec:
  scaleTargetRef:
    name: ytptube
  minReplicaCount: 0
  maxReplicaCount: 1
  cooldownPeriod: 7200
  triggers:
    - type: external-push
      metadata:
        scalerAddress: keda-add-ons-http-external-scaler.keda:9090
        interceptorRoute: ytptube
