apiVersion: apps/v1
kind: Deployment
metadata:
  name: dagu
  namespace: dagu
  labels:
    app.kubernetes.io/name: dagu
  annotations:
    secret.reloader.stakater.com/reload: dagu-ssh
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: dagu
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dagu
    spec:
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 100000
      containers:
        - name: dagu
          image: ghcr.io/dagucloud/dagu:2.16.2@sha256:fd5ed3396783091b8b3376ea0cbaef7a25978b03806deb377d007bd4432c9825
          command: ["dagu", "start-all"]
          env:
            - name: DAGU_TZ
              value: America/New_York
            - name: DAGU_HOST
              value: "0.0.0.0"
            - name: DAGU_PORT
              value: "8080"
            - name: DAGU_HOME
              value: /dagu
            - name: DAGU_DAGS_DIR
              value: /dagu/dags
            # dags dir is a read-only configMap mount; keep the wiki store
            # (default {dags_dir}/wiki, added in 2.13.0) on the writable PVC
            - name: DAGU_WIKI_DIR
              value: /dagu/wiki
            - name: DAGU_BASE_CONFIG
              value: /dagu/base.yaml
            - name: DAGU_AUTH_MODE
              value: none
            - name: HOME
              value: /dagu
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: data
              mountPath: /dagu
            - name: base
              mountPath: /dagu/base.yaml
              subPath: base.yaml
              readOnly: true
            - name: dags
              mountPath: /dagu/dags
              readOnly: true
            - name: scripts
              mountPath: /dagu/dags/scripts
              readOnly: true
            - name: ssh
              mountPath: /etc/dagu/proxmox_ed25519
              subPath: proxmox_ed25519
              readOnly: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: dagu-data
        - name: base
          configMap:
            name: dagu-base
        - name: dags
          configMap:
            name: dagu-dags
        - name: scripts
          projected:
            sources:
              - configMap:
                  name: dagu-scripts
                  # default mode 0755 so .sh files are executable
                  defaultMode: 0755
              - configMap:
                  name: dagu-env
              - secret:
                  name: dagu-pbs-keys
                  items:
                    - key: appdata.key
                      path: keys/appdata.key
                    - key: cloud-pbs.key
                      path: keys/cloud-pbs.key
                    - key: k3s-nfs.key
                      path: keys/k3s-nfs.key
                    - key: personal-files.key
                      path: keys/personal-files.key
        - name: ssh
          secret:
            secretName: dagu-ssh
            items:
              - key: proxmox_ed25519
                path: proxmox_ed25519
            defaultMode: 0400
