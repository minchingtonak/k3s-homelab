apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # porkbun-sealed-secret.yaml -- add this after running kubeseal to generate the SealedSecret
  - helmrepository-lego-webhook.yaml
  - helmrelease-lego-webhook.yaml
  - clusterissuer-letsencrypt-staging.yaml
  - clusterissuer-letsencrypt.yaml
  - certificate-wildcard.yaml
  - default-tlsstore.yaml
